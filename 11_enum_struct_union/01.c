#include <stdio.h>

enum Days {Sunday, Monday, Tuesday, Wednesday, Thursday, Friday, Saturday};

int main() {
    printf("Sunday ning qiymati: %d\n", Sunday);    // Sunday = 0
    printf("Friday ning qiymati: %d\n", Friday);    // Friday = 5
    return 0;
}
